export const registrationBoard = (state) =>
  state.registrationBoard.registrationBoard;

export const reultBoard = (state) => state.reultBoard.reultBoard;

export const dataUser = (state) => state.registrationBoard.dataUser;
